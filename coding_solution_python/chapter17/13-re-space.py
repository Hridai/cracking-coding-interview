"""
Problem:
Oh, no! You have accidentally removed all spaces, punctuation, and
capitalization in a lengthy document. A sentence like "I reset the computer.
It still didn't boot!" has been altered to "iresetthecomputeritstilldidntboot".
You have a dictionary of valid words. Given this string, find the best way to
re-insert spaces to reconstruct the original sentence. Output a sentence where
all the words are valid dictionary words. If there are multiple possible
answers, return the one with the fewest unrecognized characters.

EXAMPLE:
Input:  "jesslookedjustliketimherbrother"
Output: "jess looked just like tim her brother" (7 unrecognized chars)
"""

"""
Questions:


Algorithm:


"""

def func_to_write():
    pass


def test_func(x, expected_result):
    res = func_to_write()
    if res != expected_result:
        raise ValueError(f"Result {res} does not match expected result {expected_result}")


if __name__ == "__main__":
    test_func("x", "expected_result")
